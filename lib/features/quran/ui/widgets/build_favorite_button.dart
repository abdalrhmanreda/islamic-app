import 'package:flutter/material.dart';

class BuildFavoriteButton extends StatelessWidget {
  const BuildFavoriteButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
